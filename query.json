event {
  "Title":"..." <- length,
  "idevent":... <- existance,
  "token_user":... <- authorization,
  "Description":"..." <- length,
  "date_start":date <- validity, before date_end,
  "date_end":date <- validity, after date_start,
  "other_unknown_stuff":"..." <- pass,
  "location": {
    "postcode":...<-validity,
    "street":"..."<-length,
    "number":...<-validity,
    "city":"..."<-length,
    "name":"..."<-length,
    "country":"..."<-length,
    "other_unknown_stuff":"..." <- pass
  }
  "type":"..." <- length,
  "image":"..." <- validity,
  "organizer":{
    "id":... <- existance,
    ...
  }
  "status":... <- validity,
}
